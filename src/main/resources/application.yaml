server:
  port: 4000
  servlet:
    context-path: /api/v1/hele-desk

spring:
  application:
    name: AI-Help-Desk
  mail:
    host: smtp.gmail.com
    port: 587
    username: "govindaekbote7@gmail.com"
    password: "ppjxtkwchxhlybyd"
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
  redis:
    host: localhost
    port: 6379

  ai:
    ollama:
      base-url: http://localhost:11434
      model: llama3
      options:
        temperature: 0.7
        top_p: 0.9
        num-predict: 500
    embeddings:
      model: sentence-transformers/all-MiniLM-L6-v2
    max-token: 2000
    timeout: 30000


  datasource:
    url: jdbc:mysql://localhost:3306/ai_help_desk
    username: root
    password: Govinda@1997
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

jwt:
  secret: ldkfdkfdsdslfdsklfdsklfkldfjkdfnfdklslklfdsmkl


logging:
  file:
    name: ai-help-desk.log
  level:
    root: INFO
    org.springframework.web: DEBUG

